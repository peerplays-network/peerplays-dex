import {defaultQuote} from "./networkParams";

export const defaultSettings = {
    language: 'en',
    darkTheme: false,
    advancedMode: true,
    notifications: true,
    walletLock: 0,
    nodeAutoselect: true,
    defaultAsset: defaultQuote
};